# Custom Rules for hummane-api

- **Deployments:** The Vercel project is integrated with GitHub. Do NOT run manual Vercel CLI deployments (`npx vercel --prod`). Only commit and push changes to GitHub (`git push`), which automatically triggers the Vercel production deployment.
